describe("SeNotifications", function () {
	"use strict";

	beforeEach(module("seNotifications.service", function() {
	}));
	beforeEach(inject(function () {
	}));

	it("no-tests-yet", inject(function () {
	}));
});
